# 本机 PTE Coach

**最新算法状态：第二轮改进已加入时间约束和上下文复核，但人工评分录音验证尚未通过，自动发音纠错建议暂不启用。录音、文字核对及音素对照仍可使用。详见 [算法改进与验证](docs/算法改进与验证.md)。**

已为这台 RTX 5060 Laptop（8GB）电脑配置本地 GPU 运行。

**更新：已接入本地音素分析，以及自定义单词和音标练习。详见 [本地发音与音标练习](docs/本地发音与音标练习.md)。音标专项仍是实验功能，孤立音素可能漏识别或误识别，不能保证每次认对。**

## 日常使用

如果使用完整便携包，解压后双击 `start-portable.cmd` 即可；普通源代码下载请按 `docs/分享与安装.md` 安装 Python、FFmpeg 和依赖。

1. 双击本目录的 `start-windows.cmd`，会启动服务并打开浏览器。
2. 页面地址：http://127.0.0.1:8765 。建议在桌面 Chrome 或 Edge 中录音。
3. 输入英文原文，点击“开始录音”。首次使用时，由你允许浏览器使用麦克风。
4. 读完点击“结束录音”，试听后点击“分析这次练习”。也可以上传已有音频。
5. 查看内容差异、语速、停顿、逐词复听和音高曲线。“训练记录”可重新打开以往结果。
6. 不用时双击 `stop-windows.cmd` 释放模型和显存。关闭网页本身不会停止后台服务。

正在分析时，停止脚本会要求先等分析完成。重复启动会打开已有服务，不会再加载一份模型。未设置开机自启动。

## 本地模式的范围

- small.en 模型、NVIDIA 运行库和依赖已经放在本机；启动时固定使用 CUDA / float16。
- 云端评测和 LLM 均关闭，应用不上传录音。无需 API 密钥。
- 可用：转写、词边界、内容对照、停顿、语速、音高、规则教练和历史保存。
- 可用本地音素对照、疑似差异提示和片段复听。发音及韵律尚无校准分数。
- Practice Score 是未校准的训练指标，不能换算成 PTE 官方成绩。

## 数据与维护

- 训练数据在本目录的 `data` 文件夹。停止服务后备份整个文件夹，数据库和音频都要保留。
- 不要单独移动应用或删除 `.venv`、`models`、`run-local.py`。此虚拟环境使用本机 Codex 提供的 Python 3.12.14 运行时。
- 启动异常时查看 `logs/server.err.log`。
- 安装版本记录在 `requirements-local-lock.txt`；已校验的安装包保存在 `.cache/wheels`，可用于离线重装。
- 原桌面 ZIP 未修改。

## 首次部署验证（2026-09-16；新增功能见上方说明）

- 已识别 NVIDIA GeForce RTX 5060 Laptop GPU，8151 MiB 显存，驱动 610.88。
- GPU 确认支持 float16，已实际运行 small.en 转写；不是仅检测到了显卡。
- 24 项自动测试通过，依赖一致性检查通过，前端 JavaScript 语法检查通过。
- 5.42 秒合成英语语音完整分析成功，首次请求约 13.09 秒（含加载等开销）；此为单次部署测试，不是速度保证或发音准确率验证。
- 网页首页、分析结果、词时间戳、波形、音高和历史重开已检查。
- 停止、重启后，原测试记录仍可读取。合成测试记录未进入公开仓库；验证结论见 `docs/validation.md`。
- 实际麦克风权限、真实朗读与个体发音有效性仍待你录制一次来验收。

修复了 Windows 默认编码导致评分配置加载失败的问题，以及 SQLite 连接未关闭导致的文件占用问题。

GPU 依赖参考：[faster-whisper 官方说明](https://github.com/SYSTRAN/faster-whisper#gpu)。
